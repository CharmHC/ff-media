<ui:FluentWindow x:Class="FFMedia.App.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="FFMedia" Width="1000" Height="640"
                 WindowStartupLocation="CenterScreen"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="FFMedia">
            <ui:TitleBar.Header>
                <ui:Button Icon="{ui:SymbolIcon WeatherMoon24}"
                           Appearance="Transparent"
                           HorizontalAlignment="Right" Margin="0,0,8,0"
                           ToolTip="Toggle light/dark theme"
                           Command="{Binding ToggleThemeCommand}" />
            </ui:TitleBar.Header>
        </ui:TitleBar>

        <!-- NOTE (WPF-UI 4.3.0 adaptation): NavigationView derives from Control (not
             ContentControl) in this version and has no settable Content property -
             it owns its own internal content presenter. Selecting a MenuItemsSource
             entry navigates via its TargetPageType, resolved through DI by the
             INavigationViewPageProvider wired in MainWindow.xaml.cs / App.xaml.cs. -->
        <ui:NavigationView Grid.Row="1" x:Name="RootNavigation"
                           MenuItemsSource="{Binding MenuItems}"
                           FooterMenuItemsSource="{Binding FooterMenuItems}"
                           IsBackButtonVisible="Collapsed"
                           PaneDisplayMode="Left" />
    </Grid>
</ui:FluentWindow>
